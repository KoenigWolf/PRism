// Re-export from new locations for backward compatibility
// New imports should use @/config directly
export { PESO_COLORS, type MediaType } from "@/config/peso";
export { SOURCE_TYPES, type SourceType } from "@/config/source-types";
